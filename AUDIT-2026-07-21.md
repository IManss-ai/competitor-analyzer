# Adversarial Audit — 2026-07-21

Run: wf_5f6a007a-82f | 202 agents (106 completed, 96 hit Fable 5 limit) | 7,012,592 subagent tokens

59 raw findings -> 29 survived 3-lens adversarial verification. Round 1 complete; rounds 2-3 and completeness critic aborted on usage limit.

## 1. [CRITICAL] read_only user triggers paid LLM plan + geo generation via GET war-room endpoint (paywall bypass / cost leak)
**File:** `app/routes/campaigns.py:98`

GET /api/v1/campaigns/{campaign_id} (get_war_room) authenticates with require_api_user (NOT require_write_access) and then unconditionally calls get_or_generate_plan(campaign, db) and get_or_check_visibility(campaign, ...). Both do paid DeepSeek (llm.MODEL) calls when no fresh cache exists — get_or_generate_plan generates whenever there is no cached plan or new intel arrived (planner/engine.py:258-298), and get_or_check_visibility calls _live_check on a >7-day-old or missing snapshot (geo/visibility.py:86-99). A locked read_only user (free_test_used=True, not paying) simply loading the war-room page therefore fires up to two paid model calls. This violates the invariant that read_only accounts never trigger paid writes. The sibling mutating endpoints create_campaign and regenerate_plan ARE gated with require_write_access (return 402), and the battlecard detail/generate GETs guard their AI call inline via allow_ai=not is_read_only — but the war-room GET has no is_read_only check at all. It slips past tests/test_write_gate_coverage.py because that structural sweep only inspects POST/PUT/PATCH/DELETE routes, and no inline guard was added here (unlike the documented battlecard exception). This was partially addressed on 2026-07-02 (create/regenerate gated) but the GET war-room leak, which itself generates, was left live.

**Evidence:** campaigns.py:98-112: `def get_war_room(campaign_id, db=Depends(get_session), user_id: str = Depends(require_api_user)): campaign = _own_campaign(...); plan = get_or_generate_plan(campaign, db); ... geo = get_or_check_visibility(campaign, campaign.user_product or "your product", db)`. Repro output for a locked user (free_test_used=True, PAYWALL_ENABLED, no comp): war-room GET -> status 200, _ai_plan called: True, _live_check called: True; same user on POST /regenerate -> status 402. planner/engine.py:273 `result = _ai_plan(signals)` (llm.MODEL) reached on first load (latest is None); geo/visibility.py:61 `client...model=llm.MODEL` reached when cache stale/missing.

## 2. [CRITICAL] extract_main_content strips the sidecar's headline/Pricing/CTA sections from every stored snapshot
**File:** `app/pipeline/fetcher.py:155`

The sidecar's /scrape output is serialize() markdown (scraper-service/src/schema.ts): '# <headline>\n\n## Pricing\n<pricing>\n\n## Features\n- ...\n\n## CTA\n<cta>\n\n## Content\n<body>', with sections separated by exactly one blank line. scanner.py:54 runs every fetched page through extract_main_content, which splits on '\n\n' and DROPS any paragraph of 200 chars or less. Concrete failure: competitor headline is 'Ship faster with Acme' (paragraph '# Ship faster with Acme' = 23 chars, always <= 200) and pricing is 'Pro $49/mo, Team $99/mo, Enterprise custom' (paragraph '## Pricing\nPro $49/mo...' ~ 55 chars). Both paragraphs are deleted before the snapshot is stored, so (a) a headline repositioning is invisible to the differ forever, (b) a $49 -> $79 price change never reaches is_meaningful_change's price-token gate because the prices are not in the stored text at all, and (c) differ._pricing_section never finds a '## Pricing' heading in any scan-path snapshot, making the section-scoped price gate (differ.py:198-202) dead code and re-enabling the whole-page 'Save $200 banner' false-positive path it was built to prevent. The heuristic was written for the old Jina prose output ('Strip navigation... from Jina-extracted text') and was never updated for the structured-markdown sidecar. Core detection product silently misses concise pricing and headline changes.

**Evidence:** fetcher.py:155 `paragraphs = [p.strip() for p in raw_text.split("\n\n") if len(p.strip()) > 200]` applied at scanner.py:54 `main_content = extract_main_content(raw_text)`; sidecar sections are short paragraphs: schema.ts:26-40 `lines = ['# ' + headline, '', '## Pricing', pricing, '', ...].join('\n')` then `.replace(/\n{3,}/g, '\n\n')`

## 3. [HIGH] Transient fetch failure creates a phantom full-page change event on the next scan
**File:** `app/pipeline/scanner.py:95`

When a fetch fails, scan_competitor still saves a Snapshot with raw_text="" (main_content is "" on error, lines 54-63). The prev_snapshot query for the NEXT scan selects the latest snapshot without filtering out error snapshots (no `Snapshot.fetch_error.is_(None)` condition). So after any transient sidecar/network failure, the next successful scan diffs the full page against an empty string. Verified at runtime: `is_meaningful_change('', page)` returns `(True, len(page))`. Result: a fabricated ChangeEvent with net_char_delta = the whole page, a paid classifier call, a paid synthesizer brief, and paid action-draft generation — plus false intel in the weekly email and battlecard cache invalidation. Failure scenario: sidecar down Monday (error snapshot saved), sidecar back Tuesday → user sees 'Competitor changed 12,000 chars' with no real change.

**Evidence:** prev_snapshot = (db.execute(select(Snapshot).where(Snapshot.competitor_id == competitor.id).where(Snapshot.id != new_snapshot.id).order_by(Snapshot.fetched_at.desc()).limit(1)).scalar_one_or_none())  # no fetch_error filter; error path stored raw_text=main_content="" at lines 54-63

## 4. [HIGH] LLM failure during job scrape closes ALL open postings and fabricates a hiring signal
**File:** `app/pipeline/job_tracker.py:246`

_extract_jobs_with_claude swallows API errors and returns {"jobs": []} (line 129-131), and also returns {"jobs": []} when llm.ai_available() is False. scrape_job_postings then treats the empty list as ground truth: seen_ids is empty, so the 'mark closed' loop sets closed_at=now on EVERY open JobPosting, and a JobSnapshot is written with total_jobs=0, closed_postings=N. The battlecard then renders 'Hiring signal: 0 open roles right now; N closed since last scan' — fabricated state corruption from a transient DeepSeek outage. The careers page itself was fetched fine (the len<100 guard passed), so nothing distinguishes 'no jobs' from 'extraction failed'. Failure scenario: weekly scheduler run during a DeepSeek outage → competitor with 12 tracked roles gets all 12 closed and a false 'they closed 12 roles' strategic signal persisted and served for a week.

**Evidence:** except Exception as e: note_degraded("job_tracker.extract", "empty", "api_error", e); return {"jobs": []}  ...then...  for p in still_open: if p.posting_id not in seen_ids: p.closed_at = now; closed_count += 1  ... db.add(JobSnapshot(... total_jobs=total_jobs, closed_postings=closed_count ...)); db.commit()

## 5. [HIGH] War-room GET triggers paid plan generation and geo check for paywalled read-only users
**File:** `app/routes/campaigns.py:98`

GET /api/v1/campaigns/{campaign_id} (get_war_room) is gated only by require_api_user with no is_read_only check, and unconditionally calls get_or_generate_plan(campaign, db) and get_or_check_visibility(...). Both make paid DeepSeek calls (planner/engine.py _ai_plan, geo/visibility.py _live_check) when caches are stale or new intel exists. This violates the project invariant that read-only (free-test-used, non-paying) users never trigger paid model calls: the battlecard detail endpoint (api_v1.py:676-677) explicitly passes allow_ai=not is_read_only(viewer), and POST /regenerate is gated with require_write_access, but this GET does the identical paid work ungated. Failure scenario: a locked user (free_test_used=True, no subscription) opens the war-room page for any campaign after new intel arrives (or after the 7-day geo cache expires); each page load fires one paid plan generation and/or one paid geo call, forever, with no metering. The structural write-gate test only sweeps POST/PUT/PATCH/DELETE, so this GET is invisible to it (the only documented GET exception is battlecards/generate).

**Evidence:** campaigns.py:99-112: `def get_war_room(... user_id: str = Depends(require_api_user)): ... plan = get_or_generate_plan(campaign, db) ... geo = get_or_check_visibility(campaign, campaign.user_product or "your product", db)` — no is_read_only/allow_ai gate, unlike api_v1.py:676 `allow_ai = not (viewer is not None and is_read_only(viewer))`.

## 6. [HIGH] Planner cache invalidates on minor_copy/initial_scan events, causing repeated paid regenerations
**File:** `app/planner/engine.py:100`

get_or_generate_plan regenerates (one paid DeepSeek call) whenever _latest_intel_at(signals) is newer than the cached plan's generated_at. But _latest_intel_at takes max(detected_at) over ALL ChangeEvents from the last 30 days with no change_type filter (the _gather_signals query at lines 48-53 has no filter either) — including minor_copy, no_change, and initial_scan events. The battlecard cache guard was explicitly fixed for exactly this (battlecard.py:51-62: "with the edit-magnitude differ, rotating page content emits minor_copy events every scan, which would otherwise trigger a paid regen"), but the planner's cache never got the same filter despite its header claiming "same rules as battlecards". Failure scenario: a competitor whose homepage rotates content (testimonials, ticker) emits a minor_copy ChangeEvent on every scheduled scan; every subsequent war-room page view then sees newest_intel > latest.generated_at and fires a fresh paid plan generation plus writes a new ActionPlan row — one paid call per view per scan cycle, exactly the credit-drain class the battlecard cache guard exists to prevent.

**Evidence:** engine.py:100-102: `def _latest_intel_at(signals): times = [e.detected_at for e in signals["events"] if e.detected_at]; return max(times) if times else None` with events from lines 48-53 unfiltered by change_type; contrast battlecard.py:56-62 `ChangeEvent.change_type.notin_(("minor_copy", "no_change", "initial_scan"))`.

## 7. [HIGH] Free-test metering bypass: paid battlecard generation via detail page never sets free_test_used
**File:** `app/routes/api_v1.py:677`

free_test_used is set in exactly one place: GET /battlecards/generate/{id} (battlecard.py:945-946), after a successful generation. But the competitor detail endpoint calls get_or_generate_battlecard(comp, db, allow_ai=allow_ai) where allow_ai=True for any "full" user — which per access.py:41-42 includes every user who has NOT yet used their free test. So a brand-new free user who opens competitor detail pages (never touching /generate) triggers a real paid generation per competitor, cached with their one-test consumption never recorded. They remain access_level "full" indefinitely: every new-intel arrival regenerates paid cards on detail-page views, the war room works, and the weekly-brief scheduler (scheduler.py:118-120 gates on access_level == "full", which includes not-yet-tested) also makes paid battlecard calls for them via mailer.py:100. Failure scenario: sign up free, track 10 competitors, only ever browse /competitors/{id} detail pages — unlimited paid DeepSeek battlecard generations with the "one free test" paywall never engaging. Secondary inverse bug: if a card was already cached by the detail-page view, the user's first /generate call serves that cache without any paid call yet still burns their free test (battlecard.py:943-947 marks free_test_used regardless of whether generation actually ran).

**Evidence:** api_v1.py:676-677: `allow_ai = not (viewer is not None and is_read_only(viewer)); battlecard_data = get_or_generate_battlecard(comp, db, allow_ai=allow_ai)` — no free_test_used marking; grep confirms the only assignment is battlecard.py:946 `user.free_test_used = True` inside /generate.

## 8. [HIGH] Edit name/URL on competitor detail page hits wrong endpoint - changes silently lost
**File:** `frontend/src/app/(dashboard)/competitors/[id]/competitor-detail-client.tsx:189`

saveCompetitorSettings PATCHes `${apiUrl}/api/v1/local/competitors/${comp.id}` with `{ name: editName, url: editUrl }`, but the backend handler (app/routes/local_business.py:15 update_local_competitor) only applies google_maps_url, instagram_handle, facebook_page, and business_type - it ignores `name` and `url` entirely and still returns 200. The frontend then optimistically writes editName/editUrl into local state (lines 198-201) and closes the editor, so the UI shows the rename as saved. Failure scenario: user clicks Edit on any competitor (SaaS or local), changes the name or URL, clicks Save - the request succeeds, the header updates, but on page reload (or anywhere else the competitor renders) the old name/URL are back and scans keep hitting the old URL. The correct endpoint is the generic PATCH /api/v1/competitors/{id} (app/routes/api_v1.py:430), which does accept name and url.

**Evidence:** Frontend: `const res = await fetch(`${apiUrl}/api/v1/local/competitors/${comp.id}`, { method: 'PATCH', ... body: JSON.stringify({ name: editName, url: editUrl }) });` then on res.ok: `setDetail((prev) => ({ ...prev, competitor: { ...prev.competitor, name: editName, url: editUrl } }))`. Backend local_business.py update_local_competitor only has: `if "google_maps_url" in payload: ... if "instagram_handle" in payload: ... if "facebook_page" in payload: ... if "business_type" in payload: ...` - no name/url handling.

## 9. [HIGH] No FK ondelete/cascade anywhere: user/competitor hard delete is impossible and orphans rows
**File:** `/var/www/html/competitor-analyzer/app/models.py:42`

Every foreign key in the schema is a bare ForeignKey with no ondelete, and no SQLAlchemy relationship()/cascade is declared anywhere, so there is no ORM-level cascade either. On Postgres, `DELETE FROM users WHERE id=...` (or db.delete(user)) fails with an FK violation from competitors/campaigns/approved_actions/magic_link_tokens; deleting a competitor row fails from 9 child tables (snapshots, change_events, reviews, review_snapshots, social_posts, job_postings, job_snapshots, battlecard_cache, campaigns). Both app-level delete endpoints (app/routes/competitors.py:91 and app/routes/api_v1.py:579) only soft-delete (`c.active = False`) precisely because hard delete is impossible, and account deletion has to be done via hand-written cascade scripts that must enumerate every child table in the right order — a script that misses a table added later (e.g. battlecard_cache or campaigns) either fails mid-delete or leaves orphaned rows. change_events.snapshot_before_id/snapshot_after_id FKs to snapshots additionally make any future snapshot pruning/retention job fail. Fix: add ondelete="CASCADE" (or SET NULL for competitors.app_id) to the FK columns plus an Alembic migration recreating the constraints.

**Evidence:** app/models.py:42 `user_id = Column(UUID(as_uuid=True), ForeignKey("users.id"), nullable=False)` — same bare pattern on all 15 FKs (lines 60, 69, 71-72, 82, 89-90, 100, 115, 126, 139, 157, 166, 179-180, 190, 200, 212, 251, 263); migrations 001/003/004/007/etc. all emit `sa.ForeignKeyConstraint([...], [...])` with no ondelete. Corroborated by repeated ops pain: QA cascade-delete cleanup scripts hand-authored (project memory 2026-07-21: "Account deletion gap documented as recurring operational pitfall").

## 10. [HIGH] Model-migration drift: 4 users columns exist in no migration, only in an error-swallowing startup hack
**File:** `/var/www/html/competitor-analyzer/main.py:38`

users.password_hash, users.scan_schedule, users.email_notifications, and users.digest_email are declared in app/models.py (lines 10, 23-25) but appear in NO Alembic migration (grep across alembic/ returns zero hits). They are created only by _apply_column_guards() in main.py, which runs raw `ALTER TABLE users ADD COLUMN ...` at app startup and treats every failure as non-fatal (`print(f"...Column guard statement failed (non-fatal)...")`). Failure scenarios: (1) any environment that applies migrations without booting the FastAPI app — `alembic upgrade head` in CI, a restored DB, a one-off script with PYTHONPATH=/app querying User — gets UndefinedColumn on every `SELECT ... FROM users`, i.e. login/auth is completely broken; (2) if the guard hits a transient DB error at startup, the app boots anyway and serves 500s on every users query with only a stdout print as evidence; (3) alembic autogenerate against a migrations-only DB emits spurious add_column diffs. Password auth (`/auth/direct-login`) depends on password_hash existing, so this is the login-critical column living outside the migration chain. Fix: add a real migration for these 4 columns (idempotent IF NOT EXISTS guard for existing prod), then delete the users entries from _apply_column_guards.

**Evidence:** main.py:41-52 columns_to_add = { "users": [("password_hash", "VARCHAR", None), ..., ("scan_schedule", "VARCHAR", "'weekly'"), ("email_notifications", "BOOLEAN", "TRUE"), ("digest_email", "VARCHAR", None), ...] } and main.py:78-79 `except Exception as e: print(f"[startup] Column guard statement failed (non-fatal): {stmt}...")`. alembic/versions/001_initial.py creates users with only 7 columns; no later migration adds these 4 (`grep -rn password_hash alembic/` is empty).

## 11. [HIGH] Failed-fetch snapshots (raw_text='') become the diff baseline, guaranteeing a phantom full-page change event on recovery
**File:** `app/pipeline/scanner.py:95`

When a fetch fails, scanner.py:54-64 still persists a Snapshot with raw_text='' and fetch_error set. The previous-snapshot query at lines 95-104 selects the latest snapshot with NO filter on fetch_error. Concrete failure: scan N — sidecar and direct fallback both fail (e.g. competitor site 403s) -> empty snapshot stored; scan N+1 — page fetches fine -> is_meaningful_change('', full_page) hits differ.py:99-100 `if not a_norm or not b_norm: return max(len(a_norm), len(b_norm))` = thousands of chars > CHANGE_THRESHOLD -> a ChangeEvent is created for a page that never changed, the classifier + synthesizer + action generator all burn paid DeepSeek calls on it, and the user gets a false 'competitor repositioned' brief and action drafts in the weekly email. The same happens in reverse (page -> '' when the sidecar returns empty text with no error). Fix direction: exclude fetch_error snapshots (and empty raw_text) from the baseline query.

**Evidence:** scanner.py:58-63 `new_snapshot = Snapshot(..., raw_text=main_content,  # '' on error ... fetch_error=error)` then scanner.py:96-101 `select(Snapshot).where(Snapshot.competitor_id == competitor.id).where(Snapshot.id != new_snapshot.id).order_by(Snapshot.fetched_at.desc())` — no fetch_error filter

## 12. [HIGH] Direct-HTTP fallback stores a differently-serialized snapshot with no provenance, causing double phantom diffs on any sidecar blip
**File:** `app/pipeline/fetcher.py:91`

When the sidecar errors once (deploy restart, 503 overload from the queue cap, transient DeepSeek failure -> index.ts:153 502), fetch_page_text silently switches to raw HTML tag-stripping: whitespace-collapsed single-line text truncated to 10,000 chars, including nav/footer/cookie-banner text that Readability/llm-scraper would have removed. This is a completely different serialization of the same page than the sidecar's structured markdown, and the Snapshot row records nothing about which path produced it (fetch_error stays None, note_degraded only logs). Concrete failure: scan N sidecar-markdown snapshot; scan N+1 sidecar restarting -> direct-HTTP snapshot -> full-page phantom diff -> false ChangeEvent + brief + email; scan N+2 sidecar back -> second full-page phantom diff -> second false event. The fetcher's own comment (lines 79-83) acknowledges 'each flip reads as a phantom full-page diff' for the timeout case but the fallback path itself still flips serializations. Team observation from Jun 15 ('Sidecar-down state causes false change detection via truncation mismatch') is still unfixed. Fix direction: tag snapshots with fetch source and refuse to diff across sources, or store the fallback fetch as an error snapshot.

**Evidence:** fetcher.py:91-101 `note_degraded("fetcher", "direct_http", "sidecar_down", scraper_err)` then `text_content = re.sub(r'\s+', ' ', text_content).strip(); return text_content[:10000], None` — returned with error=None, indistinguishable from a sidecar fetch downstream

## 13. [MEDIUM] /trends/metrics counts change_type values that are never written — feature/positioning breakdown is permanently zero
**File:** `app/routes/api_v1.py:916`

api_trends_metrics builds the type_breakdown by querying ChangeEvent.change_type == "new_feature" (line 916) and == "positioning_shift" (line 923), but the classifier's VALID_CATEGORIES are {pricing_change, feature_add, repositioning, minor_copy, no_change} (app/pipeline/classifier.py:6) and nothing in the codebase ever writes "new_feature" or "positioning_shift" (verified by grep — only these two queries and frontend label maps reference them). The legacy HTML trends page (app/routes/trends.py:68) uses the correct labels, confirming the drift. Failure scenario: a competitor ships features all quarter (feature_add events exist in DB); the Trends 'change type breakdown' chart shows 0 for New feature and Positioning every week — only pricing_change and minor_copy ever register.

**Evidence:** .where(ChangeEvent.change_type == "new_feature")  /  .where(ChangeEvent.change_type == "positioning_shift")  vs classifier.py VALID_CATEGORIES = {"pricing_change", "feature_add", "repositioning", "minor_copy", "no_change"}

## 14. [MEDIUM] GET war room triggers paid plan regeneration and paid GEO check for locked (read_only) users
**File:** `app/routes/campaigns.py:107`

get_war_room is guarded only by require_api_user and calls get_or_generate_plan(campaign, db), which fires a paid DeepSeek call (_ai_plan) whenever new intel arrived since the last plan, and get_or_check_visibility, which fires another paid call when the 7-day geo cache expires. Unlike the battlecard detail path (api_v1.py:674-677 passes allow_ai=False for read_only users) and unlike POST /campaigns/{id}/regenerate (require_write_access), there is no is_read_only check here. The write-gate structural test only sweeps mutating methods, so this GET escapes it. Failure scenario: a user burns their one free test, stays locked/read_only, but keeps opening the war-room page weekly; every visit after new ChangeEvents or geo-cache expiry triggers paid model calls indefinitely — exactly the per-page-view cost leak the battlecard cost guard was built to stop.

**Evidence:** def get_war_room(campaign_id: str, db: Session = Depends(get_session), user_id: str = Depends(require_api_user)): ... plan = get_or_generate_plan(campaign, db) ... geo = get_or_check_visibility(campaign, ...)  — planner/engine.py regenerates via paid _ai_plan when newest_intel > latest.generated_at; no read_only/allow_ai gate anywhere on this path

## 15. [MEDIUM] Competitor detail page does paid battlecard generation without ever consuming the free test
**File:** `app/routes/api_v1.py:677`

GET /competitors/{id}/detail computes allow_ai = not is_read_only(viewer) and calls get_or_generate_battlecard(comp, db, allow_ai=allow_ai). For a user who has NOT used their free test, access_level is 'full', so allow_ai=True and a paid AI battlecard is generated and cached — but free_test_used is only ever set in /battlecards/generate (battlecard.py:945-947), not here. Failure scenario: a free user adds 7 competitors and only ever opens detail pages (never clicking Generate): each detail view triggers a paid generation per competitor (refreshing every 7 days / on new intel), the user is never marked free_test_used, never locks to read_only, and gets ongoing paid battlecards for free — a cost-guard bypass of the 'one free test' model.

**Evidence:** viewer = db.get(User, user_uuid); allow_ai = not (viewer is not None and is_read_only(viewer)); battlecard_data = get_or_generate_battlecard(comp, db, allow_ai=allow_ai)  — no free_test_used marking; access.py: 'full' while free_test_used is False

## 16. [MEDIUM] Model returning "rating": null crashes the review loop and rolls back the whole platform scrape
**File:** `app/pipeline/review_scraper.py:210`

sentiment is computed as `rev.get("rating", 3) >= 4`. The default of 3 only applies when the key is MISSING; when the LLM emits an explicit "rating": null (common for unrated review sites), this becomes `None >= 4` → TypeError. The exception is caught by the outer per-platform handler (line 251-254) which does db.rollback() and continues — so ALL reviews extracted for that platform, plus its ReviewSnapshot, are silently discarded. Failure scenario: Trustpilot page yields 20 reviews, one of them with rating null → entire Trustpilot scrape for that competitor is lost this run and the battlecard shows stale/no complaints. Same-file precedent: review IDs got the str-coercion fix (line 187-191) for exactly this class of LLM-output looseness; rating was missed.

**Evidence:** sentiment = "negative" if is_complaint else ("positive" if rev.get("rating", 3) >= 4 else "neutral")  ... except Exception as e: logger.warning(...); db.rollback(); continue

## 17. [MEDIUM] Malformed UUID path params return 500 instead of 4xx on multiple owner-scoped endpoints
**File:** `app/routes/api_v1.py:575`

uuid.UUID(competitor_id)/uuid.UUID(action_id) is called unguarded inside the handler on several endpoints: PATCH /competitors/{id} (line 434), POST /competitors/{id}/probe-careers (line 472), DELETE /competitors/{id} (line 575), GET /competitors/{id}/reviews (line 725), POST /queue/{id}/approve (line 807), and in local_business.py update_local_competitor and get_social_posts. FastAPI does not convert ValueError, so `DELETE /api/v1/competitors/not-a-uuid` returns a raw 500 Internal Server Error. The team's intended behavior is explicit in the same codebase: trigger_local_scan (local_business.py:143-147) wraps the parse with the comment 'A malformed id is a client error (404), not a ValueError-500', and api_competitor_detail (api_v1.py:587-590) returns 400. Failure scenario: frontend passes an undefined/truncated id → 500s pollute error monitoring and the client shows a crash state instead of Not Found.

**Evidence:** c = db.execute(select(Competitor).where(Competitor.id == uuid.UUID(competitor_id), ...))  — unguarded; contrast local_business.py: 'A malformed id is a client error (404), not a ValueError-500'

## 18. [MEDIUM] GEO visibility falsely records DeepSeek results as engine "chatgpt"
**File:** `app/geo/visibility.py:102`

_live_check asks DeepSeek (llm.MODEL = deepseek-v4-flash) who it would recommend, then get_or_check_visibility persists the snapshot with engine="chatgpt", source="live". The war-room API then returns geo.engine to the frontend, so the product tells users their AI-visibility share was measured on ChatGPT when no OpenAI call ever happened (the codebase is DeepSeek-only by decision). This is fabricated provenance of the same class the team repeatedly scrubbed elsewhere (fabricated prices, fabricated changes, honest-labeling of estimated data). Failure scenario: any user with a campaign and a real DEEPSEEK_API_KEY views the war room; the GEO widget claims a live ChatGPT recommendation share of e.g. 7/10 that ChatGPT never produced — a false factual claim about a third-party product surfaced in a paid feature.

**Evidence:** visibility.py:99-102: `live = _live_check(user_product, competitor_name, comp.business_type)` (which calls `client.chat.completions.create(model=llm.MODEL, ...)`) then `engine, source = "chatgpt", "live"`.

## 19. [MEDIUM] Local battlecard heuristic fabricates review complaints with zero review data, served on the public endpoint
**File:** `app/routes/battlecard.py:229`

_generate_local_battlecard falls back to hardcoded fake weaknesses when no complaints exist: "Slow service during peak hours mentioned in recent reviews.", "Inconsistent quality flagged across multiple reviewers.", "Limited weekend availability frustrates regulars." — asserted as coming from reviews even when the competitor has zero reviews in the DB (is_baseline True). The SaaS variant explicitly removed this exact fabrication (lines 590-594: "presenting invented weaknesses as fact is the same dishonesty as inventing a change (issue #3, bug #1) — leave them empty"), gating filler weaknesses on change_list_texts, but the local variant was never fixed. Failure scenario: anyone (including anonymous visitors and crawlers) hits GET /api/v1/battlecards/public/{id} for an uncached local competitor with no scraped reviews; _generate(comp, db, allow_ai=False) runs this path and the public share page states specific fabricated customer complaints about a real named business — a defamation-adjacent data-honesty bug on an unauthenticated surface.

**Evidence:** battlecard.py:229-234: `if not weaknesses: weaknesses = ["Slow service during peak hours mentioned in recent reviews.", "Inconsistent quality flagged across multiple reviewers.", "Limited weekend availability frustrates regulars."]` — unconditional, unlike the SaaS guard at lines 594-599 (`if not weaknesses and change_list_texts:`).

## 20. [MEDIUM] Trends type-breakdown chart permanently shows 0 for feature and positioning changes (wrong change_type names queried)
**File:** `app/routes/api_v1.py:916`

The /trends/metrics type_breakdown queries count `ChangeEvent.change_type == "new_feature"` (line 916) and `== "positioning_shift"` (line 923), but the classifier - the only writer of change_type - emits only VALID_CATEGORIES = {"pricing_change", "feature_add", "repositioning", "minor_copy", "no_change"} (app/pipeline/classifier.py:6). Nothing in the codebase ever writes "new_feature" or "positioning_shift". Failure scenario: a competitor ships feature_add and repositioning changes; the Trends page's stacked bar chart (frontend/src/components/trends-type-breakdown.tsx, Bar dataKey="new_feature" / "positioning_shift", fed via app/(dashboard)/trends/page.tsx -> api.getTrendsMetrics()) renders 0 for both series every week - only pricing and minor-copy bars ever appear, understating competitor activity. Fix belongs in the backend query (or add both spellings), listed here because the visible defect is the frontend chart.

**Evidence:** .where(ChangeEvent.change_type == "new_feature") / .where(ChangeEvent.change_type == "positioning_shift") vs classifier.py: VALID_CATEGORIES = {"pricing_change", "feature_add", "repositioning", "minor_copy", "no_change"}; frontend consumes it in trends-type-breakdown.tsx:62-63: <Bar dataKey="new_feature" .../> <Bar dataKey="positioning_shift" .../>

## 21. [MEDIUM] Hydration hazard: Signal Board renders clock-derived formatTimeAgo/signal without gate or suppressHydrationWarning
**File:** `frontend/src/app/(dashboard)/dashboard/dashboard-client.tsx:1254`

The Signal Board row renders `{formatTimeAgo(c.last_scanned)}` in a plain <span> with no `mounted` gate and no suppressHydrationWarning. formatTimeAgo (line 502) computes from `new Date()` at render time, so the server-rendered text (Vercel UTC, render time T) and the client's first hydration render (user's clock, T+delta) can differ - e.g. server emits "59m ago", client computes "1h ago", or a client clock behind the server yields "-2m ago" - triggering React #418, the exact bug class this codebase eradicated via useMounted (see use-mounted.ts). The two other formatTimeAgo call sites in this same file were patched with suppressHydrationWarning (line 1315 `<time suppressHydrationWarning ...>` and line 1420 `<span suppressHydrationWarning ...>`); line 1254 was missed. Same class: the `{c.signal}` value rendered at line 1252 derives from `Date.now()` via eventSignal (line 1081) and can also round differently between server and client renders.

**Evidence:** Line 1254: `<span className="font-mono text-xs text-right text-muted-foreground">{formatTimeAgo(c.last_scanned)}</span>` - contrast with line 1315: `<time suppressHydrationWarning className=...>{formatTimeAgo(event.detected_at)}</time>` and line 1420: `<span suppressHydrationWarning ...>{formatTimeAgo(comp.last_scanned)}</span>`. formatTimeAgo: `const now = new Date(); const diffMs = now.getTime() - d.getTime();`

## 22. [MEDIUM] War room plan-item status: optimistic update never checks response or reverts on failure
**File:** `frontend/src/app/(dashboard)/campaigns/[id]/war-room-client.tsx:95`

setStatus applies the optimistic setRoom update first, then fires the POST to /api/v1/plan-items/{itemId}/status with no res.ok check, no catch, and no rollback. Failure scenario: a read_only user (every non-paying user post-onboarding is read_only per the paywall default-deny gate) or any 401/403/network failure - the request fails silently while the UI permanently shows the item as done/dismissed (strikethrough, done-count incremented, dismiss button gone). The user believes their plan progress is saved; on reload everything reverts to pending. An unhandled fetch rejection (network down) also escapes as an uncaught promise rejection since there is no try/catch.

**Evidence:** // optimistic update\n    setRoom({ ...room, plan: { ...room.plan, items: room.plan.items.map((i) => (i.id === itemId ? { ...i, status } : i)) } });\n    await fetch(`${API_BASE}/api/v1/plan-items/${itemId}/status`, { method: 'POST', headers, body: JSON.stringify({ status }) });  // no res.ok check, no revert, no try/catch

## 23. [MEDIUM] Onboarding scan poll spins forever when backend loses the job (in-memory SCAN_JOBS + no unknown/timeout handling)
**File:** `frontend/src/app/(dashboard)/dashboard/dashboard-client.tsx:169`

The onboarding step-1 poll fetches /api/v1/scan/status/{job_id} every 3s and only exits on status 'done' or 'error'. The backend stores job state in an in-process dict (SCAN_JOBS, api_v1.py:525) and returns {"status": "unknown"} for any job_id it does not know. Failure scenario: Railway redeploys/restarts (or the worker recycles) while a new user's first scan is running - SCAN_JOBS is wiped, every subsequent poll returns 'unknown', statusMessages has no 'unknown' key so the modal shows 'Fetching page…' forever, and the interval never clears. The brand-new user is stuck on 'Running your initial scan...' indefinitely with no skip/escape control on that step (unlike steps 0/review which have Skip links). Needs an 'unknown' terminal branch and/or a max-poll timeout.

**Evidence:** Frontend: `const res = await fetch(`${apiUrl}/api/v1/scan/status/${onboardingJobId}`, ...); setOnboardingStatus(data.status); if (data.status === 'done') {...} else if (data.status === 'error') {...}` - no other exit; step-1 UI: `{statusMessages[onboardingStatus] || statusMessages.fetching}`. Backend: `status = SCAN_JOBS.get(job_id, "unknown")` where SCAN_JOBS is a module-level dict lost on restart.

## 24. [MEDIUM] No indexes on hot child-table FK columns (snapshots/change_events/reviews/review_snapshots/competitors.user_id)
**File:** `/var/www/html/competitor-analyzer/app/models.py:60`

Postgres does not auto-index FK columns, and neither the models nor any migration create indexes on: snapshots.competitor_id (models.py:60), change_events.competitor_id (:69), reviews.competitor_id (:100), review_snapshots.competitor_id (:115), social_posts.competitor_id (:126), job_postings.competitor_id (:139), job_snapshots.competitor_id (:166), competitors.user_id (:42), approved_actions FKs, or magic_link_tokens.user_id. Only the later-generation tables (campaigns, action_plans, apps, app_tech, battlecard_cache, competitors.app_id) got indexes. These columns drive the hottest queries in the app: the dashboard summary endpoint (app/routes/api_v1.py:184-263) runs GROUP BY Snapshot.competitor_id, GROUP BY ChangeEvent.competitor_id, and GROUP BY ReviewSnapshot.competitor_id+platform on every dashboard load; battlecard cache-freshness (_has_new_intel) filters ChangeEvent/Review/ReviewSnapshot by competitor_id + timestamp on every card view; the competitor detail page orders Snapshot by fetched_at DESC per competitor. snapshots is the worst case because each row stores the full extracted page text (raw_text Text) and grows on every scheduled scan of every competitor, so each unindexed lookup is a sequential scan over an ever-growing wide table. Failure scenario: dashboard latency degrades linearly with total snapshots/reviews across ALL users (cross-tenant table scans), not with the requesting user's data. Fix: one migration adding composite indexes, e.g. (competitor_id, fetched_at), (competitor_id, detected_at), (competitor_id, platform, snapshot_at), plus competitors.user_id.

**Evidence:** models.py:60 `competitor_id = Column(UUID(as_uuid=True), ForeignKey("competitors.id"), nullable=False)` — no index=True; contrast with :179 campaigns `index=True`. Migration 001/003/004/007 create these tables with zero op.create_index calls. Hot queries: api_v1.py:186 `select(Snapshot.competitor_id, func.max(Snapshot.fetched_at)).where(Snapshot.competitor_id.in_(comp_ids)).group_by(...)`, :205 same for ChangeEvent, :218-223 same for ReviewSnapshot.

## 25. [MEDIUM] Race on free_test_used check-then-set + battlecard_cache load-then-insert: double paid LLM calls and 500 on concurrent generate
**File:** `/var/www/html/competitor-analyzer/app/routes/battlecard.py:945`

Two read-modify-write races in the paid-generation path. (1) free_test_used: generate_battlecard reads `is_read_only(user)` (which keys off free_test_used), then performs the expensive DeepSeek generation, and only AFTER success does `if not user.free_test_used: user.free_test_used = True; db.commit()`. Two concurrent requests from a fresh free user (two competitors selected, or a double-clicked button — the UI lets you pick multiple competitors) both pass the read-only check before either commits the flag, so the 'one free test' cost guard pays for N cards. Since the entire generation (multi-second LLM call) sits between read and write, the window is not theoretical. (2) _store_cache (battlecard.py:75-83) does _load_cache -> if None: db.add(BattleCardCache(...)) -> commit against the UNIQUE index ix_battlecard_cache_competitor_id (migration 82f2df6eaed1); two concurrent generations for the same competitor (e.g. owner clicks force=true while a first generate is in flight) both see None, both insert, and the second commit raises IntegrityError -> unhandled 500 returned to the user AFTER the paid model call completed. Fix: mark free_test_used (with SELECT ... FOR UPDATE or UPDATE ... WHERE free_test_used=false returning rowcount) BEFORE generating, and use an upsert (ON CONFLICT DO UPDATE) or catch IntegrityError+retry in _store_cache.

**Evidence:** battlecard.py:935-939 `if user is not None and is_read_only(user): ...` then :943 `result = get_or_generate_battlecard(comp, db, force=force, ...)` then :944-947 `# The first generated card is "the one test" — mark it used after success.\n if user is not None and not user.free_test_used: user.free_test_used = True; db.commit()`. battlecard.py:76-79 `cached = _load_cache(comp_id, db)\n if cached is None:\n cached = BattleCardCache(competitor_id=comp_id)\n db.add(cached)` vs migration 82f2df6eaed1 `op.create_index(..., ['competitor_id'], unique=True)`.

## 26. [LOW] Failed LLM review extraction writes a zeroed ReviewSnapshot that masks real prior data
**File:** `app/pipeline/review_scraper.py:233`

When the review page fetch succeeds (len >= 100) but _extract_reviews_with_claude hits an API error, it returns {"reviews": [], "avg_rating": 0, "total_reviews": 0}; the code proceeds to write a ReviewSnapshot(avg_rating=0, total_reviews=0, complaint_count=0) anyway. Consumers that take the LATEST snapshot per platform — api_competitor_reviews (api_v1.py:737-747) and the review_trends history in api_trends_metrics (line 944-957) — then show a 0.0 rating replacing the previously-scraped real rating. Failure scenario: DeepSeek hiccup during the weekly scrape → competitor's Trustpilot card drops from '2.1 across 20 reviews' to '0.0 / 0 reviews' until the next successful run, and the trends chart shows a false rating cliff.

**Evidence:** snapshot = ReviewSnapshot(competitor_id=comp_uuid, platform=platform, avg_rating=avg_rating, total_reviews=total_reviews, ...)  where avg_rating/total come from the api_error fallback {"reviews": [], "avg_rating": 0, "total_reviews": 0}

## 27. [LOW] Fresh battlecard payloads can carry non-string weakness items (only cached reads are coerced)
**File:** `app/routes/battlecard.py:216`

Both generators seed `weaknesses` from ReviewSnapshot.top_complaints via json.loads and extend the list without type-checking items (local: lines 212-218; saas: lines 573-579). _coerce_str_list only runs on the model's OWN output and on cached reads (_read_cached_payload); the freshly-generated payload is returned to the caller directly (get_or_generate_battlecard line 867-869 returns `payload`, not the re-coerced cached copy). If the complaints-analysis model ever emits object-shaped complaint themes ({"theme": ...}), they flow uncoerced into payload['weaknesses'] on the first render — the exact React #31 crash class the coercion layer was built for (see _read_cached_payload docstring). Self-heals on subsequent cached reads, but the first (and for a free user, the one-free-test) render gets the poisoned shape.

**Evidence:** parsed_complaints = json.loads(latest_snapshot.top_complaints); if isinstance(parsed_complaints, list): weaknesses.extend(parsed_complaints)  — items never coerced on the fresh-generation return path

## 28. [LOW] synthesizer and action_generator lack the dummy-key short-circuit, making doomed API calls per change event
**File:** `app/pipeline/synthesizer.py:30`

Every other call site checks llm.ai_available() before calling (review_scraper, job_tracker, social_tracker, planner, geo, discovery, profiler, battlecard via is_dummy), but synthesize_brief, summarize_competitor_profile (synthesizer.py:30, 77) and generate_action (action_generator.py:74) call client.chat.completions.create unconditionally. With no/dummy DEEPSEEK_API_KEY the heuristic fallback does engage (no crash — invariant holds), but only after a live HTTPS request to api.deepseek.com fails, and the OpenAI SDK retries 401/connect errors twice by default. Failure scenario: local dev or a key-rotation outage; a scan detecting 5 changes makes up to 5 (synthesize) + 10 (two actions per change) doomed network round-trips with retries, adding tens of seconds of latency per pipeline run and noisy note_degraded("api_error") telemetry that masks the real cause ("dummy_key" is never reported for these modules, unlike battlecard.py:711).

**Evidence:** synthesizer.py:29-51 try/except around `await client.chat.completions.create(...)` with no `llm.ai_available()` guard; same in action_generator.py:73-86 (its except doesn't even call note_degraded).

## 29. [LOW] Queue card left-border color map keys never match real change_type values - color coding dead for pricing/feature/copy
**File:** `frontend/src/app/(dashboard)/queue/queue-manager.tsx:105`

borderLeftColor is looked up with keys `pricing`, `feature`, `repositioning`, `copy`, but action.change_event.change_type carries the classifier taxonomy: `pricing_change`, `feature_add`, `repositioning`, `minor_copy` (app/pipeline/classifier.py VALID_CATEGORIES; the adjacent ChangeBadge component maps exactly those full names). Only `repositioning` ever matches; every pricing and feature queue card falls through to `|| 'var(--tone-neutral)'`. Failure scenario: a pricing_change action lands in the approval queue - its card renders with the neutral gray left border instead of the amber warning tone, so the visual severity encoding (pricing=amber, feature=green) silently never appears in either theme.

**Evidence:** const borderLeftColor = {\n  pricing: 'var(--tone-warning)',\n  feature: 'var(--tone-positive)',\n  repositioning: 'var(--tone-repositioning)',\n  copy: 'var(--tone-neutral)',\n}[action.change_event.change_type] || 'var(--tone-neutral)'; // actual values are pricing_change / feature_add / minor_copy per classifier.py and change-badge.tsx
